---
layout: base.njk
bodyClass: story-page
---
{%- set s = {
  fn: fn,
  title: title,
  dek: dek,
  kicker: kicker,
  date: date,
  readingTime: readingTime,
  cover: cover,
  coverAlt: coverAlt,
  coverCaption: coverCaption,
  eager: true
} -%}
{%- set body = content -%}
{% include "story-body.njk" %}

{# Same floating control as the in-page reader, so both routes into a story feel identical. #}
<a class="reader-close" href="/#stories" data-reader-close>
  <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5M12 19l-7-7 7-7"/></svg>
  <span>All stories</span>
</a>
